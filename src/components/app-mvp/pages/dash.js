import { element } from 'utils/dom.js';

export function Dash() {
    return (
        element('div', {className: 'dashboard'},
            element('h1', {textContent: 'Mental Load it Up!'}),
            
    
        )
    )
}
